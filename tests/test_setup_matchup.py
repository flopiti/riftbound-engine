# Removed: the matchup setup lives in the agent's set_up_matchup tool
# (riftbound/server.js), configured via /fake-fill + /reset — not an engine
# helper. This placeholder keeps the file importable with no tests.
